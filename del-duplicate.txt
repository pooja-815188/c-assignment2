#include<stdio.h>
int main()
{
    int size=5;
    int arr[]={1,2,3,2,4};
    int i,j,k;
    for(i=0;i<size;i++)
    {
        for(j=i+1;j<size;j++)
        if(arr[i]==arr[j])
        {
            for(k=j;k<size-1;k++)
            {
                arr[k]=arr[k+1];
            }
            size--;
            j--;
        }
    }
    for(i=0;i<size;i++)
    printf("%d\n",arr[i]);
}
